import MobileCommand from "@/components/mobile-command";

export const metadata = {
  title: "Brokie Command",
  description: "Mobile command center for The Brokie brand."
};

export default function CommandPage() {
  return <MobileCommand />;
}
